[project]
name = "tokenfiche"
version = "0.1.0"
description = "Render large text payloads into dense page images and probe multimodal context limits."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "pillow>=10",
  "tiktoken>=0.7",
]

[tool.ruff]
line-length = 100

